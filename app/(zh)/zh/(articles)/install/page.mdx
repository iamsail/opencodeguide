export const metadata = {
  title: 'OpenCode 安装教程（Mac / Linux / Windows）',
  description: 'OpenCode 官方安装方式全解析。支持 Desktop App、curl、brew、npm 等多种方式，包含国内网络与 API Key 配置说明。',
  alternates: {
    canonical: '/zh/docs/intro',
  },
}

# OpenCode 安装教程（官方方式全解析）

OpenCode 是一个 **开源的 AI Coding Agent**，支持 **命令行（CLI）和桌面端（Desktop App）** 两种使用方式。

官方提供了多种安装方式，覆盖不同系统与用户习惯。你可以根据自己的情况选择最合适的一种。

---

## 安装方式总览（官方支持）

<div className="my-6 w-full overflow-y-auto"><table className="w-full border-collapse"><thead><tr className="m-0 border-t p-0 even:bg-muted border-gray-200 dark:border-gray-800"><th className="border px-4 py-2 text-left font-bold border-gray-200 dark:border-gray-800">安装方式</th><th className="border px-4 py-2 text-left font-bold border-gray-200 dark:border-gray-800">适合人群</th><th className="border px-4 py-2 text-left font-bold border-gray-200 dark:border-gray-800">是否需要 Node.js</th></tr></thead><tbody><tr className="m-0 border-t p-0 even:bg-muted border-gray-200 dark:border-gray-800"><td className="border px-4 py-2 text-left border-gray-200 dark:border-gray-800">Desktop App</td><td className="border px-4 py-2 text-left border-gray-200 dark:border-gray-800">普通用户 / 非开发者</td><td className="border px-4 py-2 text-left border-gray-200 dark:border-gray-800">❌</td></tr><tr className="m-0 border-t p-0 even:bg-muted border-gray-200 dark:border-gray-800"><td className="border px-4 py-2 text-left border-gray-200 dark:border-gray-800">curl 一行命令</td><td className="border px-4 py-2 text-left border-gray-200 dark:border-gray-800">macOS / Linux 用户</td><td className="border px-4 py-2 text-left border-gray-200 dark:border-gray-800">❌</td></tr><tr className="m-0 border-t p-0 even:bg-muted border-gray-200 dark:border-gray-800"><td className="border px-4 py-2 text-left border-gray-200 dark:border-gray-800">Homebrew / paru</td><td className="border px-4 py-2 text-left border-gray-200 dark:border-gray-800">熟悉系统包管理器的用户</td><td className="border px-4 py-2 text-left border-gray-200 dark:border-gray-800">❌</td></tr><tr className="m-0 border-t p-0 even:bg-muted border-gray-200 dark:border-gray-800"><td className="border px-4 py-2 text-left border-gray-200 dark:border-gray-800">npm / bun</td><td className="border px-4 py-2 text-left border-gray-200 dark:border-gray-800">前端 / Node.js 开发者</td><td className="border px-4 py-2 text-left border-gray-200 dark:border-gray-800">✅</td></tr></tbody></table></div>

---

## 方式一：安装 Desktop App（最推荐）

如果你不关心环境配置，**直接使用桌面端是最省事的方式**。

OpenCode 官方提供 Desktop App，支持：

- macOS
- Windows
- Linux

下载安装后即可使用，无需 Node.js、无需命令行。

> **Windows 开发者提示：** 虽然桌面应用很方便，但为了获得最佳的命令行工具兼容性和文件系统性能，我们强烈建议 Windows 用户通过 **WSL (Windows Subsystem for Linux)** 运行 OpenCode。
>
> 👉 查看 [Windows (WSL) 运行指南](/zh/docs/windows-wsl) 获取详细教程。

👉 官方下载地址：https://opencode.ai

---

## 方式二：使用 curl 安装（macOS / Linux）

这是官方推荐的 CLI 安装方式之一，不依赖 Node.js。

```bash
curl -fsSL https://opencode.ai/install | bash
```

安装完成后，重新打开终端，验证是否成功：

```bash
opencode --version
```

---

## 方式三：使用系统包管理器安装

### macOS（Homebrew）

```bash
brew install opencode
```

### Arch Linux（paru）

```bash
paru -S opencode
```

---

## 方式四：通过 npm / bun 安装（开发者）

如果你已经有 Node.js 环境，也可以通过 npm 或 bun 安装 CLI。

### 环境要求

* Node.js v18.0.0 或更高
* Git

检查 Node.js 版本：

```bash
node -v
```

### 安装命令

```bash
npm install -g @opencode-ai/cli
# 或
bun add -g @opencode-ai/cli
```

如果遇到权限问题（EACCES）：

```bash
sudo npm install -g @opencode-ai/cli
```

---

## 初始化配置（国内用户必看）

安装完成后，运行初始化命令：

```bash
opencode init
```

常见选项说明：

* Provider

  * Ollama：本地模型
  * OpenAI / DeepSeek：在线 API
* Language

  * 选择 Chinese，可让 Agent 优先使用中文

---

## 设置 API Key（如使用在线模型）

将以下内容写入 `~/.zshrc` 或 `~/.bashrc`：

```bash
export OPENAI_BASE_URL="https://api.deepseek.com/v1"
export OPENAI_API_KEY="sk-xxxxxxxx"
```

保存后执行：

```bash
source ~/.zshrc
```

---

## 验证安装是否成功

```bash
opencode run "创建一个 demo.txt 文件，并写入：你好 OpenCode"
```

---

## 常见问题（FAQ）

### command not found: opencode

通常是全局安装路径未加入 PATH。

```bash
npm config get prefix
```

确保该路径下的 `bin` 目录在 PATH 中。

---

### 国内网络慢 / 依赖下载失败

```bash
npm config set registry https://registry.npmmirror.com
```

---

## 下一步

* 本地模型部署：[OpenCode + Ollama](/zh/ollama-local-deploy)
* 协议原理介绍：[什么是 MCP？](/zh/what-is-mcp)
